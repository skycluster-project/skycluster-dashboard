# SkyCluster FrontEnd

### Manual Deployment

Ensure you have npm installed. We test and build the porject using `npm` version `18.20.8`. Use `nvm`:

```bash
nvm install 18.20.8
```


Then:

```bash
npm install -D 

# First build the (may take some time)
npm run build

npm run dev --serve
```